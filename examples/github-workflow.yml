name: SkillProof

on:
  pull_request:
  push:
    branches: [main]

jobs:
  audit-skills:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npx --yes skillproof check . --profile codex --format github --smoke --strict
